const Upload = () => {
  return (
    <section>
      <h1 className="text-lg font-semibold tracking-wide text-cyan-300">Evidence Upload</h1>
      <p className="mt-2 text-sm text-slate-400">Ingest forensic artifacts for parsing.</p>
    </section>
  );
};

export default Upload;
